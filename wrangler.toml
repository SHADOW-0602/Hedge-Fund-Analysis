name = "shmventures"
compatibility_date = "2024-01-01"

# Use 'routes' array for custom domains to avoid conflicts
routes = [
  { pattern = "www.shmventures.org", custom_domain = true }
]

[build]
command = "python -m pip install --upgrade pip && pip install -r requirements.txt"